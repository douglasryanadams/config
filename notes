A File for things I have to look up from time to time but can't quite remember:

keytool -import -alias {alias} -file {file_location.cert} -keystore /usr/lib/jvm/{java_home}/jre/lib/security/cacerts
tar -czv --exclude=config/.git -f config.tar.gz config/
ldapsearch -x -H "ldap://{domain}:{port}" -D "uid={username},cn=xxx,cn=xxx,dc=xxx,dc=xxx,dc=xxx" -W -b "cn=xxx,cn=xxx,dc=xxx,dc=xxx,dc=xxx"

